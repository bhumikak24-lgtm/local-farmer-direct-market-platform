stages:
  - backend
  - frontend

backend:
  stage: backend
  image: python:3.12
  script:
    - cd backend
    - pip install -r requirements.txt
    - python manage.py check

frontend:
  stage: frontend
  image: node:20
  script:
    - cd frontend
    - npm install
    - npm run build